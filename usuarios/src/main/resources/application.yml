spring:
  datasource:
    url: jdbc:postgresql://localhost:5420/usuarios_pedidos_db
    username: usuarios_pedidos
    password: user123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8082
