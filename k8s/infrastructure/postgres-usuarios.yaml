apiVersion: v1
kind: Secret
metadata:
  name: usuarios-postgres-secret
type: Opaque
stringData:
  POSTGRES_USER: usuarios_pedidos
  POSTGRES_PASSWORD: user123
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: usuarios-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-usuarios
spec:
  serviceName: postgres-usuarios
  replicas: 1
  selector:
    matchLabels:
      app: postgres-usuarios
  template:
    metadata:
      labels:
        app: postgres-usuarios
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: usuarios_pedidos_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: usuarios-postgres-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: usuarios-postgres-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: usuarios-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-usuarios
spec:
  selector:
    app: postgres-usuarios
  ports:
    - name: tcp
      port: 5432
      targetPort: 5432
