apiVersion: v1
kind: Secret
metadata:
  name: payments-postgres-secret
type: Opaque
stringData:
  POSTGRES_USER: payments_pedidos
  POSTGRES_PASSWORD: user123
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: payments-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-payments
spec:
  serviceName: postgres-payments
  replicas: 1
  selector:
    matchLabels:
      app: postgres-payments
  template:
    metadata:
      labels:
        app: postgres-payments
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: payments_pedidos_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: payments-postgres-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: payments-postgres-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: payments-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-payments
spec:
  selector:
    app: postgres-payments
  ports:
    - name: tcp
      port: 5432
      targetPort: 5432
