apiVersion: v1
kind: Secret
metadata:
  name: entregas-app-secret
type: Opaque
stringData:
  DB_USERNAME: entregas_pedidos
  DB_PASSWORD: user123
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: entregas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: entregas
  template:
    metadata:
      labels:
        app: entregas
    spec:
      containers:
        - name: entregas
          image: entregas:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8086
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: kubernetes
            - name: DB_URL
              value: jdbc:postgresql://postgres-entregas:5432/entregas_pedidos_db
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: entregas-app-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: entregas-app-secret
                  key: DB_PASSWORD
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: messaging-config
                  key: SPRING_KAFKA_BOOTSTRAP_SERVERS
            - name: LOG_LEVEL
              value: INFO
            - name: DDL_AUTO
              value: update
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 40
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: entregas
spec:
  selector:
    app: entregas
  ports:
    - name: http
      port: 8086
      targetPort: 8086
