info:
  name: RegistrarProducto
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8083/api/catalogo/productos
  body:
    type: json
    data: |-
      {
        "restauranteId": "08401a8b-d47a-4bdb-b7e1-5cec03ef59a8",
        "nombre": "Pizza Americana",
        "descripcion": "Pizza personal",
        "precio": 15.0
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
